---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: medishift-gateway
  namespace: medishift-prod
spec:
  gatewayClassName: eg
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: Same

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: medishift-frontend-route
  namespace: medishift-prod
spec:
  parentRefs:
    - name: medishift-gateway
      namespace: medishift-prod
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: medishift-frontend-service
          port: 3000

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: medishift-auth-route
  namespace: medishift-prod
spec:
  parentRefs:
    - name: medishift-gateway
      namespace: medishift-prod
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/auth
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /api/auth
      backendRefs:
        - name: medishift-auth-service
          port: 3001

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: medishift-staff-route
  namespace: medishift-prod
spec:
  parentRefs:
    - name: medishift-gateway
      namespace: medishift-prod
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/staff
      backendRefs:
        - name: medishift-staff-service
          port: 3002
    - matches:
        - path:
            type: PathPrefix
            value: /api/departments
      backendRefs:
        - name: medishift-staff-service
          port: 3002

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: medishift-shift-route
  namespace: medishift-prod
spec:
  parentRefs:
    - name: medishift-gateway
      namespace: medishift-prod
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/shifts
      backendRefs:
        - name: medishift-shift-service
          port: 3003

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: medishift-leave-route
  namespace: medishift-prod
spec:
  parentRefs:
    - name: medishift-gateway
      namespace: medishift-prod
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/leaves
      backendRefs:
        - name: medishift-leave-service
          port: 3004
